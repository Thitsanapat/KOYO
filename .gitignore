# Confidential HEX20 source documents - never commit, even to a private repo
koyosource/

# Unrelated coursework, not part of the KOYO decoder deliverable
6 COMM/

# Downloaded SatNOGS data (frames, audio, waterfalls) - large, regenerable via fetch_satnogs.py
data/

# Superseded duplicate raw audio/waterfall files (35MB) - the useful history is in
# archive/mislabeled_non_koyo_data/, which stays tracked
archive/superseded_data/

# Python
.venv/
__pycache__/
*.pyc

# Task Scheduler logs and lock file
logs/
refresh_koyo.lock

# Local Grafana + InfluxDB stack - downloaded binaries and runtime data only;
# start.ps1/stop.ps1, provisioning/, grafana-dashboards/, load_influx.py stay tracked
local-stack/influxdb/
local-stack/influxdb-data/
local-stack/grafana/
local-stack/grafana-data/
local-stack/grafana-data-startup.log

# OS junk
.DS_Store
._*

# Private correspondence - never commit
MAIL_LOREN.pdf
MAIL_LOREN.docx
~$*.docx

# SCION-X: other people's cloned repos / downloaded resources - not ours to
# redistribute. Our own SCION-X code/notes (once any exist) should live
# directly under scionx/, not under scionx/external/.
scionx/external/
